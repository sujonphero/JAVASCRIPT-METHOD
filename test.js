const objects = {
    name: 'sujon',
    ano: 'sheikh',
    mor: 'minhajul',
    other: 'programmer'
}
for(item in objects){
    console.log(objects[item])
}

objects.map((item) => console.log(item))
// console.log(result)
